#ifndef TYPES_HPP
#define TYPES_HPP

#include <cstdint>
#include <iostream>

using TIStream = std::istream;
using TOStream = std::ostream;
using FEnum = uint16_t;
using FRaw = uint8_t;
using FPointer = void*;
using FReal = double;
using FSize = size_t;
using FBool = bool;
using FNatural = unsigned int;
using FInteger = int;
using FReturn = int;

#endif